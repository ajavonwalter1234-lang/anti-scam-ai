fastapi>=0.95.0
uvicorn[standard]>=0.22.0
pydantic>=1.10.0
pyyaml>=6.0
python-multipart>=0.0.6

# Optional (heavy) ML deps for later when you want real model-backed processors:
# transformers>=4.0.0
# torch>=1.13.0
# torchaudio>=0.13.0
